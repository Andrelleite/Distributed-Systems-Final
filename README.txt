FUNCTIONAL REQUIREMENTS FOR THIS PROJECT:

-> TOMCAT V.8.5
-> JSOUP LIBRARY
-> META1 SERVER INTERFACES

Must copy and past all folder to new project or importa as a new one.

First Run RMI SERVER and then Multicast, they must connect by 224.x.x.x ip address. Any must work.
Then run TOMCAT web server. This will make the project able to run in all senses. 
Don't forget to change localhost in tomcat to your machine's IP addres, since RMI will do that automatically.
Use https://192.168.x.x:8443/Hey and start using UCBusca!

